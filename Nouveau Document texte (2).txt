:root{
    --couleur-primaire: #6C63FF;
    --couleur-texte: #111217;
    --couleur-cta: #111217;
    --espace-horizontal: 40px;
}

*{
    box-sizing:border-box;
}

html,body{
    margin:0;
    padding:0;
    font-family:Inter, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    color:var(--couleur-texte)
}

.barre-navigation{
    display: flex;
    align-items:center;
    justify-content:space-between;
    padding:18px 48px;
    background:#ffffff;
    gap:20px;
}

.logo{
    display:inline-flex;
    align-items:center;
    text-decoration:none
}
.logo-bk{
    font-weight:800;
    color:#000;
    margin-right:6px;
    font-size:20px
}
.logo-creative{
    color:var(--couleur-primaire);
    font-weight:700;
    font-size:20px
}

.menu-principal{
    display:flex;
    gap:28px;
    list-style:none;
    margin:0;
    padding:0
}
.element-menu a{
    color:var(--couleur-texte);
    text-decoration:none;
    font-weight:600;
    letter-spacing:0.2px
}
.element-menu a:hover{
    color:var(--couleur-primaire)
}

.bouton-cta{
    display:inline-block;
    background:var(--couleur-cta);
    color:#fff;
    padding:12px 18px;
    border-radius:4px;
    text-decoration:none;
    font-weight:700;
    box-shadow:0 2px 6px rgba(17,18,23,0.12);
}
.bouton-cta:hover{
    transform:translateY(-1px);
    transition:transform .15s ease
}

/* Responsive adjustments */
@media (max-width:900px){
    .menu-principal{gap:18px}
    .barre-navigation{padding:14px 20px}
}

@media (max-width:720px){
    .nav-centre{display:none}
    .barre-navigation{justify-content:space-between}
    .logo-bk,.logo-creative{font-size:18px}
    .bouton-cta{padding:10px 14px}
}

/* Small polish for alignment */
.nav-gauche,.nav-centre,.nav-droite{
    display:flex;
    align-items:center
}

/* Utility for visually matching original spacing */
.menu-principal .element-menu:first-child{
    margin-left:0
}

/* Début styles section hero : styles pour la bannière d'introduction */
.hero{
    background: #efe6ff; /* couleur lavande douce */
    padding:60px var(--espace-horizontal);
}
.hero-contenu{
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:40px;
    max-width:1200px;
    margin:0 auto;
}
.hero-texte{
    flex:1;
    min-width:280px
}
.role{color:rgba(0,0,0,0.6);
    margin:0 0 14px 0
}
.hero-titre{
    font-size:40px;
    margin:0 0 8px 0;
    color:#0f0f15
}
.hero-nom{
    font-size:38px;
    margin:0 0 18px 0;
    color:var(--couleur-primaire);font-weight:800}
.nom-highlight{color:var(--couleur-primaire)}
.hero-description{max-width:560px;line-height:1.6;color:rgba(0,0,0,0.7);margin-bottom:22px}

.hero-boutons{display:flex;gap:16px}
.btn{display:inline-block;padding:12px 22px;border-radius:6px;text-decoration:none;font-weight:700}
.btn-primaire{background:#111217;color:#fff}
.btn-secondaire{background:transparent;color:#111217;border:2px solid #111217}

.hero-image{width:420px;display:flex;justify-content:center}
.hero-image img{max-width:100%;height:auto;border-radius:8px;box-shadow:0 12px 30px rgba(17,18,23,0.12)}

/* Responsive : empiler le contenu en mobile */
@media (max-width:900px){
    .hero-contenu{gap:24px}
    .hero-titre{font-size:34px}
    .hero-nom{font-size:30px}
}

@media (max-width:720px){
    .hero-contenu{flex-direction:column-reverse;align-items:center;text-align:center;padding-top:20px}
    .hero-image{width:260px}
    .hero-description{max-width:unset}
    .hero-boutons{justify-content:center}
}

/* Fin styles section hero */
